<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Thème sombre dès le démarrage : aucun flash blanc -->
    <style name="Theme.RoxiPlayer" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/roxi_bg</item>
        <item name="android:colorBackground">@color/roxi_bg</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@color/roxi_bg</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
